# @atiende/mcp-billing

Servidor MCP reservado (hoteles), a portar de `hoteles/packages/mcp-servers/billing` — distinto de `packages/billing` de plataforma (facturación al cliente de Atiende). Aún no portado — ver `docs/REQUISITOS.md`.
