# @atiende/db

Reservado — motor de migraciones y engines de conexión (PGlite / embedded-postgres / Postgres gestionado), a consolidar desde `hoteles/packages/db`. Aún no portado.

Cuando se porte, `packages/db/migrations/` seguirá la numeración por bloques del diseño del monorepo: `0000–0099` núcleo (`core-tenancy`), `0100–0199` restaurantes, `0200–0299` hoteles, `0300–0399` citas, `0400–0499` licitaciones, `0500–0599` rentas, `0900+` reservado para `packages/billing`.
