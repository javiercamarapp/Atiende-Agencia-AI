# @atiende/domain-licitaciones

Reservado — portado de `licitaciones/packages/agents` (lo específico de negocio, sin el gateway LLM que ya vive fusionado en `@atiende/agent-core`). Aún no portado — ver `docs/REQUISITOS.md`.
