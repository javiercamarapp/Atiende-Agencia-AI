# Wiring del gateway LLM

Reservado para instanciar `GatewayRouter` (de `@atiende/agent-core`) por organización/carril dentro de `apps/api`. Aún no construido — el gateway en sí ya existe en `packages/agent-core/src/gateway`.
