# apps/api

Reservado — un solo backend Hono para todas las verticales (ver `docs/REQUISITOS.md`). Aún no construido: no hay `package.json` ni `server.ts` en esta carpeta todavía. El middleware de auth/tenancy que consumirá ya existe en `@atiende/core-auth`/`@atiende/core-tenancy`.
